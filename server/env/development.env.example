NODE_ENV=development
PORT=3000
HOST=localhost

JET_LOGGER_MODE=CONSOLE
JET_LOGGER_FILEPATH=jet-logger.log
JET_LOGGER_TIMESTAMP=TRUE
JET_LOGGER_FORMAT=LINE

WORKER_WEB_SERVER_URL=http://localhost:3000

POSTGRES_DB=match_uploader
POSTGRES_USER=match_uploader

CRONTAB_FILE_PATH=./src/crontab.txt
###########################################
# Values below this are generally the only ones you need to set when using the standard docker-compose.yml setup:

POSTGRES_PASSWORD=

# Format: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
DB_CONNECTION_STRING=
